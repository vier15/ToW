using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mbee : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Long live Leithania");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}